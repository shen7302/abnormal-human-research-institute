一、算法原理
 
该版本实现PageRank网页排名算法，采用幂迭代法求解网页链接转移矩阵的主特征向量。
PageRank核心计算公式：
 

PR_{new}(i)=\frac{1-d}{N}+d\sum_{j\rightarrow i}\frac{PR_{old}(j)}{out\_degree(j)}

 
式中：
 
- d=0.85为阻尼系数，代表用户继续点击网页链接的概率；
- N为网页总数量；
- out\_degree(j)为网页j的出链数量；
- 迭代直至所有网页PR值的变化量小于收敛阈值\varepsilon=10^{-8}，或达到最大迭代次数。
算法额外处理了出度为0的悬挂节点，避免迭代无法收敛。
 
二、程序数据结构
 
1. 网页链表： WebNode 单向链表，临时存储所有网页URL；
2. 网页图结构 WebGraph ：- 邻接矩阵 adj[MAX_NODES][MAX_NODES] 存储网页间有向链接；
- 一维数组存储每个节点出度、迭代前后的PageRank值；
- 二维字符数组建立网页编号与URL字符串的映射关系。
 
三、复杂度分析
 
（1）时间复杂度
 
设节点总数为N，有向边总数为E，实际迭代收敛次数为k。
 
1. 建图阶段：插入边时需要线性遍历完成URL到节点ID的匹配，单次查找为O(N)，全部边插入总开销为O(N\cdot E)。
2. PageRank迭代计算（核心部分）：程序采用邻接矩阵存储图，每一轮迭代需要遍历全部N\times N矩阵元素，单次迭代时间为O(N^2)。经过k轮迭代收敛后，迭代部分总时间为O(k\cdot N^2)。
3. 排序输出：对N个网页排名采用快速排序，复杂度O(N\log N)，占比极小。
 
整体时间复杂度：
 

T(N)=O\left(N\cdot E + k\cdot N^2\right)

 
优化方案：将邻接矩阵改为邻接表存储入边集合，迭代开销可以由O(N^2)降至O(E)，整体复杂度优化为O(N\cdot E + k\cdot E)，更适合稀疏网页网络图。
 
（2）空间复杂度
 
程序的主要内存开销来自二维邻接矩阵 adj[MAX_NODES][MAX_NODES] ，占用空间为O(N^2)；其余出度数组、PR数组、URL映射数组均为线性空间O(N)。
因此程序整体空间复杂度：
 

S(N)=O(N^2)

 
四、版本小结
 
1. 本版本基于幂迭代法成功实现PageRank排序，能够对小规模网页链接图完成权重计算并输出排名结果。
2. 采用邻接矩阵存储图结构时，算法时间复杂度为O(k\cdot N^2)，空间复杂度为O(N^2)，适合教学实验与小数据集验证。
3. 面向真实海量网页场景，必须将存储结构替换为邻接表，优化边遍历与URL查找逻辑，将复杂度降低至线性级别，从而提升算法运行效率。
 
参考自【PageRank算法简介-哔哩哔哩】 https://b23.tv/PyO62yt
